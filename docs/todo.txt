- asynchronously prep the next video file for encoding. especially useful if
  there's a file with VFR that we need to count frames on in the background

- make async function to determine average fps being encoded on swarm

- make x265-builder image for global deployment

- figure out windows madness

- ansible: for redis host, set host system parameters to stop warnings:
  echo never > /sys/kernel/mm/transparent_hugepage/enabled
  echo never > /sys/kernel/mm/transparent_hugepage/defrag
  sudo sysctl vm.overcommit_memory=1

